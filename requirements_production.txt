# EmoSense Global API Deployment
# Production requirements for cloud deployment

# FastAPI and server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# ML dependencies (production versions)
torch>=2.0.0,<3.0.0
transformers>=4.35.0,<5.0.0
scikit-learn>=1.3.0,<2.0.0
numpy>=1.24.0,<2.0.0

# Optional ML libraries (install if available)
# These will use fallbacks if not available
# nltk>=3.8
# librosa>=0.10.0
# opencv-python>=4.8.0

# Cloud deployment support
gunicorn>=21.2.0
psutil>=5.9.0

# Security and monitoring
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Environment management
python-dotenv>=1.0.0
